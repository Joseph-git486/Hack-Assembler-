# Hack-Assembler-
A Hack assembler built in Python that runs on your terminal. It takes a Hack Assembly (.asm) file as input and produces the equivalent 16-bit binary machine code (.hack) file. Supports A/C instructions, labels, variables, and predefined symbols.
